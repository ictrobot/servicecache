#!/usr/bin/env bash
# binaryen's wasm-opt, with -g: sc_init points wasixcc's
# WASIXCC_BINARYEN_LOCATION here, so every wasm-opt run wasixcc makes keeps
# the module's name section.
exec "${SC_WASM_OPT:?sc_init must set SC_WASM_OPT}" -g "$@"
