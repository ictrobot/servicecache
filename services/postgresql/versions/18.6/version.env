# Per-version overrides for build.sh (upstream URL, tag, dependency versions).

# The server imports the ictrobot_shm_v1 shared-memory extension
SC_GUEST_EXTENSIONS="ictrobot_shm_v1"

# The server links OpenSSL for TLS, SCRAM and the SHA and HMAC functions
OPENSSL_VERSION=3.5.8
