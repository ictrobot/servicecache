diff --git a/src/cluster_legacy.c b/src/cluster_legacy.c
index 1287703..9b44f57 100644
--- a/src/cluster_legacy.c
+++ b/src/cluster_legacy.c
@@ -1185,7 +1185,7 @@ int clusterLockConfig(char *filename) {
  * and a fcntl-based solution won't help, as we constantly re-open that file,
  * which will release _all_ locks anyway
  */
-#if !defined(__sun)
+#if !defined(__sun) && !defined(__wasm__)
     /* To lock it, we need to open the file in a way it is created if
      * it does not exist, otherwise there is a race condition with other
      * processes. */
@@ -1221,7 +1221,7 @@ int clusterLockConfig(char *filename) {
     server.cluster_config_file_lock_fd = fd;
 #else
     UNUSED(filename);
-#endif /* __sun */
+#endif /* __sun || __wasm__ */
 
     return C_OK;
 }
@@ -1630,7 +1630,7 @@ void clusterHandleServerShutdown(bool auto_failover) {
     serverLog(LL_NOTICE, "Saving the cluster configuration file before exiting.");
     clusterSaveConfig(1);
 
-#if !defined(__sun)
+#if !defined(__sun) && !defined(__wasm__)
     /* Unlock the cluster config file before shutdown, see clusterLockConfig.
      *
      * This is needed if you shutdown a very large server process, it will take
@@ -1643,7 +1643,7 @@ void clusterHandleServerShutdown(bool auto_failover) {
     if (server.cluster_config_file_lock_fd != -1) {
         flock(server.cluster_config_file_lock_fd, LOCK_UN | LOCK_NB);
     }
-#endif /* __sun */
+#endif /* __sun || __wasm__ */
 }
 
 /* Reset a node performing a soft or hard reset:
diff --git a/src/debug.c b/src/debug.c
index 9b32dc6..8d8ff1f 100644
--- a/src/debug.c
+++ b/src/debug.c
@@ -42,13 +42,28 @@
 
 #include <arpa/inet.h>
 #include <signal.h>
-#include <dlfcn.h>
 #include <fcntl.h>
 #include <sys/mman.h>
 #include <unistd.h>
 
 #include "valkey_strtod.h"
 
+#ifndef __wasm__
+#include <dlfcn.h>
+#else
+typedef struct {
+    const char *dli_fname;
+    void *dli_fbase;
+    const char *dli_sname;
+    void *dli_saddr;
+} Dl_info;
+static int dladdr(const void *addr, Dl_info *info) {
+    UNUSED(addr);
+    UNUSED(info);
+    return 0;
+}
+#endif
+
 #ifdef HAVE_BACKTRACE
 #include <execinfo.h>
 #ifndef __OpenBSD__
diff --git a/src/module.c b/src/module.c
index 1ea966e..ee1bdb7 100644
--- a/src/module.c
+++ b/src/module.c
@@ -69,7 +69,33 @@
 #include "io_threads.h"
 #include "scripting_engine.h"
 #include "cluster_migrateslots.h"
+#ifndef __wasm__
 #include <dlfcn.h>
+#else
+/* WebAssembly modules are linked statically and have no dynamic loader. */
+#define RTLD_NOW 0
+#define RTLD_LOCAL 0
+static void *dlopen(const char *path, int flags) {
+    UNUSED(path);
+    UNUSED(flags);
+    errno = ENOTSUP;
+    return NULL;
+}
+static void *dlsym(void *handle, const char *symbol) {
+    UNUSED(handle);
+    UNUSED(symbol);
+    errno = ENOTSUP;
+    return NULL;
+}
+static int dlclose(void *handle) {
+    UNUSED(handle);
+    errno = ENOTSUP;
+    return -1;
+}
+static char *dlerror(void) {
+    return "dynamic module loading is unavailable on WebAssembly";
+}
+#endif
 #include <sys/stat.h>
 #include <sys/wait.h>
 #include <fcntl.h>
diff --git a/src/rdma.c b/src/rdma.c
index 8a51331..0ac55b0 100644
--- a/src/rdma.c
+++ b/src/rdma.c
@@ -13,11 +13,21 @@
  * SPDX-License-Identifier: BSD-3-Clause
  */
 
+#ifndef __wasm__
 #define VALKEYMODULE_CORE_MODULE
+#endif
 #include "server.h" // Include server.h to use serverLog.
 #include "serverassert.h"
 #include "connection.h"
 
+#ifdef __wasm__
+
+int RegisterConnectionTypeRdma(void) {
+    return C_ERR;
+}
+
+#else
+
 #if defined __linux__ && defined USE_RDMA /* currently RDMA is only supported on Linux */
 #if (USE_RDMA == 1 /* BUILD_YES */) || \
     ((USE_RDMA == 2 /* BUILD_MODULE */) && defined(BUILD_RDMA_MODULE) && (BUILD_RDMA_MODULE == 2))
@@ -1935,3 +1945,5 @@ int RegisterConnectionTypeRdma(void) {
 }
 
 #endif /* __linux__ */
+
+#endif /* __wasm__ */
diff --git a/src/server.h b/src/server.h
index 735434a..1185f55 100644
--- a/src/server.h
+++ b/src/server.h
@@ -96,6 +96,12 @@ static_assert(sizeof(off_t) >= 8, "off_t must be 64-bit; ensure _FILE_OFFSET_BIT
 
 #ifdef USE_LTTNG
 #define valkey_fork() do_fork()
+#elif defined(__wasm__)
+static inline pid_t valkeyWasmFork(void) {
+    errno = ENOTSUP;
+    return -1;
+}
+#define valkey_fork() valkeyWasmFork()
 #else
 #define valkey_fork() fork()
 #endif
diff --git a/src/tls.c b/src/tls.c
index cf0321e..c767e22 100644
--- a/src/tls.c
+++ b/src/tls.c
@@ -27,7 +27,9 @@
  * POSSIBILITY OF SUCH DAMAGE.
  */
 
+#ifndef __wasm__
 #define VALKEYMODULE_CORE_MODULE /* A module that's part of the server core, uses server.h too. */
+#endif
 
 #include "server.h"
 #include "connhelpers.h"
diff --git a/src/util.c b/src/util.c
index 5c7cd5a..0844fc5 100644
--- a/src/util.c
+++ b/src/util.c
@@ -1254,7 +1254,11 @@ sds getAbsolutePath(char *filename) {
  * i.e timezone variables are linux specific.
  */
 long getTimeZone(void) {
-#if defined(__linux__) || defined(__sun)
+#if defined(__wasm__)
+    /* WASI clocks use UTC and do not populate gettimeofday's obsolete
+     * timezone output argument. */
+    return 0;
+#elif defined(__linux__) || defined(__sun)
     return timezone;
 #else
     struct timezone tz;
