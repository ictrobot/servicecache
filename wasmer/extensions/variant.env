# Upstream Wasmer plus the fixes set and every extension's patch set: an
# extension guest runs standalone, needing nothing from the servicecache set.
# Each extension owns its set under extensions/<name>/patches.
WASMER_PATCH_SETS="wasmer/fixes/patches extensions/ictrobot_shm_v1/patches"
