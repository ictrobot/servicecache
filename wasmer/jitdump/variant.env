# Upstream Wasmer plus the fixes set, every extension's patch set and the
# jitdump set: a CLI whose --profiler jitdump profiles a guest outside the
# host. Patch sets apply in the order listed.
WASMER_PATCH_SETS="wasmer/fixes/patches extensions/ictrobot_shm_v1/patches wasmer/jitdump/patches"
