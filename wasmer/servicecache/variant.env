# The runtime the ServiceCache host embeds: the pinned tag plus the fixes
# and servicecache patch sets. Patch sets apply in the order listed.
WASMER_PATCH_SETS="wasmer/fixes/patches wasmer/servicecache/patches"
