# Upstream Wasmer plus only the fixes set: fixes that carry no ServiceCache
# machinery. The toolchain smoke runs its regression fixtures under this
# CLI, and the set builds and tests on its own.
WASMER_PATCH_SETS="wasmer/fixes/patches"
